<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{HTTPS} off
    RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

    RewriteCond %{HTTP_HOST} ^bigmike\.exposed$
    RewriteRule ^ https://www.bigmike.exposed%{REQUEST_URI} [L,R=301]
</IfModule>
